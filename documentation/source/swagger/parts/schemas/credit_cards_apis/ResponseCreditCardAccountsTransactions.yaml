type: object
required:
  - data
  - links
  - meta
properties:
  data:
    type: array
    maxItems: 1000 # todo verificar
    items:
      $ref: ./CreditCardAccountsTransaction.yaml
  links:
    $ref: ../Links.yaml
  meta:
    $ref: ../Meta.yaml
additionalProperties: false
