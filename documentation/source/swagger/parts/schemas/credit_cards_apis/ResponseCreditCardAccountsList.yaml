type: object
required:
  - data
  - links
  - meta
properties:
  data:
    type: array
    maxItems: 1000 # todo verificar
    description: Conjunto de informações de conta de pagamento pós-paga
    items:
      $ref: ./CreditCardAccountsData.yaml
    minItems: 0
  links:
    $ref: ../Links.yaml
  meta:
    $ref: ../Meta.yaml
additionalProperties: false
